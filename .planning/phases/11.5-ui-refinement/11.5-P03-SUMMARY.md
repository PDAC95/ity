---
phase: 11.5-ui-refinement
plan: "03"
subsystem: ui
tags: [react, framer-motion, tailwind, chat, dark-theme, animation]

# Dependency graph
requires:
  - phase: 11-ai-chat-wizard
    provides: Chat wizard base (chat-wizard.tsx, chat-message.tsx, chat-input.tsx, page.tsx)
provides:
  - Premium dark chat bubble UI with AI (left) and user (right) message layout
  - framer-motion fadeIn+slideUp entry animations per message
  - Progress stepper (5 steps, pastel blue highlights)
  - Typing indicator (3 pulsing dots in AI bubble during streaming)
  - Dark-styled input with pastel blue send button and focus ring
  - Dark banners for rate limit, finished state, and error states
  - Chat page header with school name display and close navigation
affects: [11.5-ui-refinement, any UI work touching chat wizard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "motion.div with initial/animate/transition for message entry animations (framer-motion already installed)"
    - "Typing indicator via animate-pulse with animationDelay stagger on span elements"
    - "Progress stepper via userTurnCount heuristic: Math.min(Math.ceil(userTurnCount / 3), 5)"
    - "Client component (ChatPageHeader) co-located in components/chat/ to keep RSC page clean"

key-files:
  created:
    - ity/apps/web/components/chat/chat-page-header.tsx
  modified:
    - ity/apps/web/components/chat/chat-message.tsx
    - ity/apps/web/components/chat/chat-wizard.tsx
    - ity/apps/web/components/chat/chat-input.tsx
    - ity/apps/web/app/(dashboard)/a/landing/chat/page.tsx

key-decisions:
  - "ChatPageHeader extracted as separate client component — page.tsx is RSC, useRouter requires 'use client'"
  - "Typing indicator shown during isStreaming (status === 'streaming' || 'submitted'), hidden when last AI message streams in (isLastAssistantMessage cursor handles that case)"
  - "Stepper uses userTurnCount heuristic (ceil(turns/3), capped at 5) — no server round-trip needed"

patterns-established:
  - "AI bubble: bg-zinc-800 border-zinc-700 rounded-xl rounded-tl-none, Bot icon in bg-zinc-700 circle"
  - "User bubble: bg-[#bfdbfe]/10 border-[#bfdbfe]/20 rounded-xl rounded-tr-none, right-aligned"
  - "All chat banners use bg-zinc-800 border-zinc-700 dark palette — no amber-50, zinc-50, red-50"

requirements-completed: [UI-CHAT, UI-PREMIUM]

# Metrics
duration: 12min
completed: 2026-04-15
---

# Phase 11.5 Plan 03: Chat Wizard Premium Redesign Summary

**Bubble-style dark chat with framer-motion entry animations, 5-step progress stepper, typing indicator, and dark-themed input and banners — eliminating all flat rows and light surfaces**

## Performance

- **Duration:** 12 min
- **Started:** 2026-04-15T17:08:03Z
- **Completed:** 2026-04-15T17:24:42Z
- **Tasks:** 2
- **Files modified:** 5 (4 modified, 1 created)

## Accomplishments
- Redesigned chat-message.tsx: AI messages as left-aligned dark bubbles (bg-zinc-800) with Bot avatar; user messages as right-aligned pastel blue tint bubbles (bg-[#bfdbfe]/10), both with framer-motion fadeIn+slideUp
- Added progress stepper (5 dots with pastel blue fill for completed/current step) and typing indicator (3 staggered pulsing dots in AI bubble) to chat-wizard.tsx
- Replaced all light surfaces with dark zinc palette: input container (bg-zinc-800/50), send button (bg-[#bfdbfe] text-zinc-900), rate limit / finished / error banners
- Created ChatPageHeader client component with school name and X close button (navigates to /a/landing/templates)

## Task Commits

Each task was committed atomically:

1. **Task 1: Chat message bubbles + entry animations** - `afb9ddc` (feat)
2. **Task 2: Chat wizard header + stepper + typing indicator + dark input** - `1564ff0` (feat)

**Plan metadata:** (see final docs commit)

## Files Created/Modified
- `ity/apps/web/components/chat/chat-message.tsx` - Bubble layout with framer-motion, Bot avatar, left/right alignment
- `ity/apps/web/components/chat/chat-wizard.tsx` - Progress stepper, typing indicator, dark banners
- `ity/apps/web/components/chat/chat-input.tsx` - Dark container, pastel blue send button, dark focus ring
- `ity/apps/web/components/chat/chat-page-header.tsx` - New: client component with school name + close button
- `ity/apps/web/app/(dashboard)/a/landing/chat/page.tsx` - Wraps wizard with ChatPageHeader in flex column

## Decisions Made
- ChatPageHeader extracted as separate `'use client'` component because page.tsx is RSC and `useRouter` requires a client context
- Typing indicator shown for entire `isStreaming` window (includes 'submitted' status) rather than only after first token — gives immediate visual feedback
- Stepper dot-and-line approach kept inline in chat-wizard.tsx — simple enough to not warrant a separate component

## Deviations from Plan

None — plan executed exactly as written. The only addition was extracting `ChatPageHeader` into its own file (`chat-page-header.tsx`) rather than inlining the client code in the RSC page, which was implied by the plan's requirement to use `useRouter` in an RSC file.

## Issues Encountered
None — TypeScript compiled cleanly on both tasks.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Chat experience now matches the dark premium theme of the rest of the dashboard
- All chat files are free of indigo, light zinc, and amber surfaces
- Ready for further UI phases (P04+) or end-to-end testing of the full wizard flow
- Verify: the `isStreaming` typing indicator does not conflict with `isLastAssistantMessage` cursor if both trigger simultaneously — both are intentional but show different indicators

---
*Phase: 11.5-ui-refinement*
*Completed: 2026-04-15*
