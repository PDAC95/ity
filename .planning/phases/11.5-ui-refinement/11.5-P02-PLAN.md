---
phase: 11.5-ui-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ity/apps/web/app/(dashboard)/dashboard-shell.tsx
  - ity/apps/web/components/dashboard/onboarding-checklist.tsx
  - ity/apps/web/components/profile/profile-form.tsx
  - ity/apps/web/components/profile/basic-info-card.tsx
  - ity/apps/web/components/profile/contact-card.tsx
  - ity/apps/web/components/profile/social-links-card.tsx
  - ity/apps/web/components/school/general-tab.tsx
  - ity/apps/web/components/school/branding-tab.tsx
  - ity/apps/web/components/school/school-setup-tabs.tsx
  - ity/apps/web/components/landing/template-gallery.tsx
  - ity/apps/web/components/landing/template-preview-modal.tsx
autonomous: true
requirements: [UI-DARK, UI-FORMS, UI-TYPO]
must_haves:
  truths:
    - "Dashboard content area has dark background (#1e1e22) — no white/light surfaces"
    - "All form inputs across profile and school setup use bg-zinc-800 border-zinc-700 with pastel blue focus ring"
    - "All CTA/submit buttons use bg-[#bfdbfe] text-zinc-900 — no indigo-600 buttons anywhere in dashboard"
    - "Template gallery active filter uses bg-[#bfdbfe] text-zinc-900 — no indigo-600"
    - "Onboarding checklist progress bar uses bg-[#bfdbfe] — no indigo-500"
    - "Notch SVG fill matches content area background (#1e1e22)"
  artifacts:
    - path: "ity/apps/web/app/(dashboard)/dashboard-shell.tsx"
      provides: "Dark content area background"
      contains: "#1e1e22"
    - path: "ity/apps/web/components/profile/profile-form.tsx"
      provides: "Pastel blue CTA button"
      contains: "bfdbfe"
    - path: "ity/apps/web/components/landing/template-gallery.tsx"
      provides: "Dark filter pills with pastel accent"
      contains: "bfdbfe"
  key_links:
    - from: "dashboard-shell.tsx content area bg"
      to: "all dashboard child pages"
      via: "layout cascade"
      pattern: "#1e1e22"
    - from: "dashboard-shell.tsx notch SVG fill"
      to: "content area bg"
      via: "visual match"
      pattern: "fill.*#1e1e22"
---

<objective>
Dark-theme the dashboard content area, all form components (profile + school setup), template gallery, and onboarding checklist. This is the highest-impact single change: the content area background flip from #f8f8f8 to #1e1e22 propagates to every dashboard page.

Purpose: The dashboard shell (header + dock) is already dark, but the content area is near-white (#f8f8f8). Forms use indigo-600 accents instead of the locked pastel blue. This plan closes the gap.

Output: Dark content area, unified pastel blue accents, dark form inputs across all dashboard forms.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.5-ui-refinement/11.5-CONTEXT.md
@.planning/phases/11.5-ui-refinement/11.5-RESEARCH.md

CRITICAL per Pitfall 4: When changing content area bg from #f8f8f8 to #1e1e22, ALSO change notch SVG fill to match.
CRITICAL per Pitfall 1: After changing content bg, grep for text-zinc-900 and text-gray-900 in dashboard child components — they will be invisible on dark bg.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark content area + onboarding + template gallery accents</name>
  <files>
    ity/apps/web/app/(dashboard)/dashboard-shell.tsx
    ity/apps/web/components/dashboard/onboarding-checklist.tsx
    ity/apps/web/components/landing/template-gallery.tsx
    ity/apps/web/components/landing/template-preview-modal.tsx
  </files>
  <action>
Read each file first, then apply:

**dashboard-shell.tsx:**
- Content area: Change `backgroundColor: '#f8f8f8'` → `backgroundColor: '#1e1e22'`
- Notch SVG: Change `fill="#f8f8f8"` → `fill="#1e1e22"` (Pitfall 4 — must match exactly)
- DO NOT change: header, dock, nav pill, mobile menu, user dropdown — these are LOCKED as correct per research audit
- After change, grep for any remaining `text-zinc-900` or `text-gray-900` within this file that would become invisible

**onboarding-checklist.tsx:**
- Progress bar fill: Replace `bg-indigo-500` / `bg-indigo-600` → `bg-[#bfdbfe]`
- Links: Replace `hover:text-indigo-400` / `text-indigo-400` → `hover:text-[#bfdbfe]` / `text-[#bfdbfe]`
- Any remaining indigo references → pastel blue equivalent
- Do NOT change bg-zinc-900 cards (already correct per research)

**template-gallery.tsx:**
- Active filter pill: Replace `bg-indigo-600 text-white` → `bg-[#bfdbfe] text-zinc-900`
- Inactive filter pill: Ensure `bg-zinc-800 text-zinc-300 hover:bg-zinc-700`
- Sticky filter bar bg: Change `bg-zinc-950` → `bg-[#1e1e22]/90 backdrop-blur-sm` (Pitfall 2 — match content area bg, not zinc-950)
- Content background references: Update any `#f8f8f8` if present

**template-preview-modal.tsx:**
- Verify dark styling. Replace any `bg-white`, `text-gray-*`, `border-gray-*` if found.
- Modal overlay: should be dark (bg-black/50 or similar)
- Modal content: should use `bg-zinc-900 border-zinc-700`
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Content area is #1e1e22. Notch SVG fill matches. Onboarding uses pastel blue progress bar. Template gallery active filter is pastel blue. No indigo-500/600 remains in these files. No #f8f8f8 remains.</done>
</task>

<task type="auto">
  <name>Task 2: Dark form inputs across profile and school setup</name>
  <files>
    ity/apps/web/components/profile/profile-form.tsx
    ity/apps/web/components/profile/basic-info-card.tsx
    ity/apps/web/components/profile/contact-card.tsx
    ity/apps/web/components/profile/social-links-card.tsx
    ity/apps/web/components/school/general-tab.tsx
    ity/apps/web/components/school/branding-tab.tsx
    ity/apps/web/components/school/school-setup-tabs.tsx
  </files>
  <action>
Read each file first, then apply these changes:

**All input fields across all 7 files:**
- Replace any `bg-white` / `bg-gray-50` → `bg-zinc-800`
- Replace `border-gray-300` / `border-gray-200` → `border-zinc-700`
- Replace `text-gray-900` / `text-gray-700` → `text-zinc-100`
- Replace `placeholder-gray-*` → `placeholder:text-zinc-500`
- Replace `focus:border-indigo-*` / `focus:border-blue-*` → `focus:border-[#bfdbfe]`
- Replace `focus:ring-indigo-*` / `focus:ring-blue-*` → `focus:ring-[#bfdbfe]/30`
- Add `focus:outline-none focus:ring-2` if missing

**profile-form.tsx specifically:**
- Submit button: Replace `bg-indigo-600 hover:bg-indigo-700` → `bg-[#bfdbfe] text-zinc-900 hover:bg-[#93c5fd]`
- Replace `text-white` on submit → `text-zinc-900`
- "Cambiar foto" link: Replace `text-indigo-400` → `text-[#bfdbfe]`
- Focus ring: Replace `ring-indigo-500` → `ring-[#bfdbfe]/30`
- **Loading state (per locked decision):** Import `Loader2` from lucide-react. When form `isSubmitting` is true: show `<Loader2 className="h-4 w-4 animate-spin" />` + text changes to "Guardando...", button is disabled via `disabled:opacity-50 disabled:cursor-not-allowed`. When not submitting: show normal label, no spinner. Pattern: `{isSubmitting ? <><Loader2 className="h-4 w-4 animate-spin" /> Guardando...</> : 'Guardar cambios'}`
- **Form validation mode (per locked decision):** Change react-hook-form `useForm()` call to include `mode: 'onBlur'` and `reValidateMode: 'onBlur'` — validates on blur + submit, NOT in real-time while typing. Apply this same mode change to ALL useForm() calls in profile-form.tsx and any other form files in this task that use useForm().

**school-setup-tabs.tsx specifically:**
- Active tab indicator: Replace any `text-indigo-*` / `border-indigo-*` → `text-[#bfdbfe]` / `border-[#bfdbfe]`
- Do NOT change bg-zinc-900 or bg-zinc-800 cards (already correct per research)

**branding-tab.tsx specifically:**
- Review color picker. Replace any indigo accents with pastel blue.
- Save/submit buttons: Same pastel blue CTA pattern with Loader2 spinner + "Guardando..." loading state.
- If branding-tab.tsx has its own useForm(), add `mode: 'onBlur', reValidateMode: 'onBlur'`.

**general-tab.tsx specifically:**
- If general-tab.tsx has its own useForm(), add `mode: 'onBlur', reValidateMode: 'onBlur'`.
- Submit button: Same loading state pattern (Loader2 + "Guardando...").

**Card hover states (per locked decision):**
- Apply `hover:border-zinc-600 hover:bg-zinc-800 transition-colors` to all card containers (bg-zinc-800/50 border-zinc-700) across all 7 files in this task. Cards should respond to hover with a subtle border and background shift.

**Labels across all files:**
- Replace `text-gray-700` / `text-gray-600` → `text-zinc-400`
- Ensure `text-sm` size

**Error messages across all files:**
- Replace `text-red-500` / `text-red-600` → `text-[#fecaca]`
- Error input border: add `border-[#fecaca]` conditional

**Secondary/help text:**
- Replace `text-gray-500` / `text-gray-400` → `text-zinc-400` / `text-zinc-500`
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All profile and school form inputs use bg-zinc-800 border-zinc-700. All submit buttons use bg-[#bfdbfe] text-zinc-900. No indigo-*, bg-white, text-gray-*, border-gray-* remain in any of the 7 form files. Tab active state uses pastel blue.</done>
</task>

</tasks>

<verification>
- Grep for `indigo` in all 11 files — should return 0 matches
- Grep for `#f8f8f8` in dashboard-shell.tsx — should return 0 matches
- Grep for `text-gray-` in all form component files — should return 0 matches
- Grep for `bg-white` in all form component files — should return 0 matches
- TypeScript compiles without errors
</verification>

<success_criteria>
Dashboard content area is dark (#1e1e22). All forms (profile, school, template gallery) use consistent dark inputs with pastel blue accent. No indigo or gray color classes remain in modified files. Notch SVG matches content bg.
</success_criteria>

<output>
After completion, create `.planning/phases/11.5-ui-refinement/11.5-P02-SUMMARY.md`
</output>
