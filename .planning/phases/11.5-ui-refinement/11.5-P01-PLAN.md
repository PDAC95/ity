---
phase: 11.5-ui-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ity/apps/web/app/(auth)/layout.tsx
  - ity/apps/web/app/(auth)/login/page.tsx
  - ity/apps/web/app/(auth)/register/page.tsx
  - ity/apps/web/app/(auth)/forgot-password/page.tsx
  - ity/apps/web/app/(auth)/reset-password/page.tsx
  - ity/apps/web/app/(auth)/verify-email/page.tsx
  - ity/apps/web/components/auth/password-input.tsx
  - ity/apps/web/components/auth/social-button.tsx
  - ity/apps/web/components/auth/divider.tsx
autonomous: true
requirements: [UI-DARK]
must_haves:
  truths:
    - "Auth pages (login, register, forgot-password, reset-password, verify-email) render with dark zinc backgrounds — no white/light surfaces visible"
    - "Auth left branding panel uses zinc-950 background with pastel blue (#bfdbfe) ITY wordmark — no blue gradient"
    - "All auth inputs use bg-zinc-800 border-zinc-700 with pastel blue focus ring"
    - "All auth CTA buttons use bg-[#bfdbfe] text-zinc-900 — no indigo/blue-600 buttons"
    - "Auth error messages use text-[#fecaca] inline — no bg-red-50 boxes"
  artifacts:
    - path: "ity/apps/web/app/(auth)/layout.tsx"
      provides: "Dark auth layout with zinc-950 branding panel"
      contains: "zinc-950"
    - path: "ity/apps/web/app/(auth)/login/page.tsx"
      provides: "Dark login form"
      contains: "bg-zinc-800"
    - path: "ity/apps/web/components/auth/password-input.tsx"
      provides: "Dark password input"
      contains: "zinc-700"
  key_links:
    - from: "ity/apps/web/app/(auth)/layout.tsx"
      to: "all auth pages"
      via: "layout wrapper"
      pattern: "bg-zinc-9(00|50)"
---

<objective>
Dark-theme all auth pages (login, register, forgot-password, reset-password, verify-email) and shared auth components (password-input, social-button, divider).

Purpose: Auth pages are the first impression — currently light/blue gradient, completely different visual language from the dark dashboard. This plan unifies them with the zinc dark theme.

Output: All 6 auth page files + 3 shared auth components restyled to dark zinc palette with pastel blue accent.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.5-ui-refinement/11.5-CONTEXT.md
@.planning/phases/11.5-ui-refinement/11.5-RESEARCH.md

Read each auth file before editing — register, forgot-password, reset-password, verify-email were not fully audited during research.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark auth layout + shared components</name>
  <files>
    ity/apps/web/app/(auth)/layout.tsx
    ity/apps/web/components/auth/password-input.tsx
    ity/apps/web/components/auth/social-button.tsx
    ity/apps/web/components/auth/divider.tsx
  </files>
  <action>
Read each file first, then apply these changes:

**layout.tsx:**
- Left branding panel: Replace `bg-gradient-to-br from-blue-600 via-blue-700 to-indigo-800` with `bg-zinc-950 border-r border-zinc-800`. ITY wordmark text color → `text-[#bfdbfe]`. Any supporting text → `text-zinc-400`.
- Right form panel: Replace white/light bg with `bg-zinc-900`. Text colors: `text-gray-*` → appropriate zinc level (headings zinc-100, body zinc-300, secondary zinc-400).
- Wrapper: Ensure outermost div has `bg-zinc-900` so mobile (no left panel) is dark too. Per Pitfall 6 from research.

**password-input.tsx:**
- Input: `bg-zinc-800 border-zinc-700 text-zinc-100 placeholder:text-zinc-500`
- Focus: `focus:border-[#bfdbfe] focus:ring-2 focus:ring-[#bfdbfe]/30 focus:outline-none`
- Toggle icon: `text-zinc-400 hover:text-zinc-200`
- Replace any `border-gray-*`, `focus:border-blue-*`, `focus:ring-blue-*`

**social-button.tsx:**
- Google button: `bg-zinc-800 border-zinc-700 text-zinc-200 hover:bg-zinc-700`
- Replace any `border-gray-300`, `bg-white`, `text-gray-700`

**divider.tsx:**
- Line: `bg-zinc-700` or `border-zinc-700`
- Text: `text-zinc-500`
- Replace any `bg-gray-300`, `text-gray-500`
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Auth layout renders dark zinc-950 left panel with pastel blue wordmark, dark zinc-900 right panel. Password input, social button, and divider all use zinc dark palette. No blue gradient, no gray-*, no white backgrounds remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Dark auth page forms (login, register, forgot-password, reset-password, verify-email)</name>
  <files>
    ity/apps/web/app/(auth)/login/page.tsx
    ity/apps/web/app/(auth)/register/page.tsx
    ity/apps/web/app/(auth)/forgot-password/page.tsx
    ity/apps/web/app/(auth)/reset-password/page.tsx
    ity/apps/web/app/(auth)/verify-email/page.tsx
  </files>
  <action>
Read each file first, then apply these mechanical substitutions across all 5 files:

**Input fields:**
- Replace `border-gray-300` / `border-gray-200` → `border-zinc-700`
- Replace `bg-white` / `bg-gray-50` → `bg-zinc-800`
- Replace `text-gray-900` / `text-gray-700` → `text-zinc-100`
- Replace `placeholder-gray-*` / `placeholder:text-gray-*` → `placeholder:text-zinc-500`
- Replace `focus:border-blue-500` / `focus:border-indigo-500` → `focus:border-[#bfdbfe]`
- Replace `focus:ring-blue-500` / `focus:ring-indigo-500` → `focus:ring-[#bfdbfe]/30`
- Add `focus:outline-none focus:ring-2` if missing

**Labels:**
- Replace `text-gray-700` / `text-gray-900` → `text-zinc-400`
- Font: `text-sm`

**CTA buttons (submit):**
- Replace `bg-blue-600 hover:bg-blue-700` / `bg-indigo-600 hover:bg-indigo-700` → `bg-[#bfdbfe] text-zinc-900 hover:bg-[#93c5fd]`
- Replace `text-white` → `text-zinc-900`
- Add: `transition-colors rounded-lg font-semibold disabled:opacity-50 disabled:cursor-not-allowed`
- **Loading state (per locked decision):** Add `Loader2` icon from lucide-react with `animate-spin` class. When form `isSubmitting` is true: show `<Loader2 className="h-4 w-4 animate-spin" />` + text changes to "Guardando...", button is disabled. When not submitting: show normal label text, no spinner. Pattern: `{isSubmitting ? <><Loader2 className="h-4 w-4 animate-spin" /> Guardando...</> : 'Iniciar sesion'}`

**Error messages:**
- Replace `bg-red-50 text-red-600` / `bg-red-100 text-red-700` → `text-[#fecaca]` (no background, inline)
- Error input border: `border-[#fecaca]`

**Links (forgot password, login/register toggle):**
- Replace `text-blue-600 hover:text-blue-500` / `text-indigo-600 hover:text-indigo-500` → `text-[#bfdbfe] hover:text-[#93c5fd]`

**Headings:**
- Replace `text-gray-900` → `text-zinc-100`

**Secondary text:**
- Replace `text-gray-500` / `text-gray-600` → `text-zinc-400`

**Any remaining `text-gray-*`:** Map to nearest zinc equivalent (900→100, 700→300, 500→400, 400→500).
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 5 auth page forms use dark zinc palette. No `text-gray-*`, `bg-white`, `bg-blue-600`, `bg-indigo-600`, `border-gray-*`, `bg-red-50` classes remain in any auth page file. All inputs bg-zinc-800, all buttons bg-[#bfdbfe], all errors text-[#fecaca].</done>
</task>

</tasks>

<verification>
- Grep for `text-gray-` in all auth files — should return 0 matches
- Grep for `bg-blue-600` in all auth files — should return 0 matches
- Grep for `bg-indigo-600` in all auth files — should return 0 matches
- Grep for `bg-white` in all auth files — should return 0 matches
- Grep for `from-blue-600` in auth layout — should return 0 matches
- TypeScript compiles without errors
</verification>

<success_criteria>
All auth pages render with consistent dark zinc theme matching the dashboard aesthetic. No light/gray/blue remnants. Pastel blue (#bfdbfe) used as sole accent color for CTAs, focus rings, and links.
</success_criteria>

<output>
After completion, create `.planning/phases/11.5-ui-refinement/11.5-P01-SUMMARY.md`
</output>
