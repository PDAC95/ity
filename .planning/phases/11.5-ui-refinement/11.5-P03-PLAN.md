---
phase: 11.5-ui-refinement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ity/apps/web/app/(dashboard)/a/landing/chat/page.tsx
  - ity/apps/web/components/chat/chat-wizard.tsx
  - ity/apps/web/components/chat/chat-message.tsx
  - ity/apps/web/components/chat/chat-input.tsx
autonomous: true
requirements: [UI-CHAT, UI-PREMIUM]
must_haves:
  truths:
    - "Chat wizard has a header showing school name, progress stepper, and close button"
    - "AI messages render as left-aligned dark bubbles (bg-zinc-800 border-zinc-700) with bot avatar"
    - "User messages render as right-aligned pastel blue tint bubbles (bg-[#bfdbfe]/10)"
    - "Messages animate in with fadeIn + slideUp (framer-motion)"
    - "Typing indicator shows three pulsing dots inside AI bubble"
    - "Chat input uses dark styling (bg-zinc-800/50 border-zinc-700) with pastel blue send button"
    - "Rate limit and finished banners use dark zinc palette — no amber-50, zinc-50, or red-50"
  artifacts:
    - path: "ity/apps/web/components/chat/chat-message.tsx"
      provides: "Left/right bubble layout with framer-motion entry"
      contains: "motion.div"
    - path: "ity/apps/web/components/chat/chat-input.tsx"
      provides: "Dark input with pastel blue send button"
      contains: "bfdbfe"
    - path: "ity/apps/web/components/chat/chat-wizard.tsx"
      provides: "Progress stepper, typing indicator, dark banners"
      contains: "stepper"
    - path: "ity/apps/web/app/(dashboard)/a/landing/chat/page.tsx"
      provides: "Chat page wrapper with header"
      contains: "schoolName"
  key_links:
    - from: "chat/page.tsx header"
      to: "chat-wizard.tsx"
      via: "page wrapper renders header + wizard"
      pattern: "ChatWizard"
    - from: "chat-wizard.tsx"
      to: "chat-message.tsx"
      via: "renders message list"
      pattern: "ChatMessage"
    - from: "chat-message.tsx"
      to: "framer-motion"
      via: "motion.div entry animation"
      pattern: "motion\\.div"
---

<objective>
Redesign the chat wizard for a premium feel: bubble-style messages (AI left, user right), framer-motion entry animations, progress stepper, typing indicator, dark-themed banners, and a polished input area.

Purpose: The chat wizard is the core product interaction — the AI conversation that builds the creator's landing page. Currently uses flat light-surface rows. This plan transforms it into a premium dark chat experience.

Output: Fully redesigned chat with header, stepper, bubble messages, typing dots, entry animations, and dark input.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11.5-ui-refinement/11.5-CONTEXT.md
@.planning/phases/11.5-ui-refinement/11.5-RESEARCH.md

Key from research:
- framer-motion already installed (^12.31.0), AnimatePresence pattern used in dashboard-shell.tsx
- Chat stepper: use turn count heuristic Math.min(Math.ceil(userTurnCount / 3), 5) as step proxy (Pitfall 5)
- Typing indicator: CSS animate-pulse + animationDelay (Pattern 4 from research)
- AI SDK v6: UIMessage.parts (not .content), no createdAt field
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat message bubbles + entry animations</name>
  <files>
    ity/apps/web/components/chat/chat-message.tsx
  </files>
  <action>
Read the file first. Then redesign completely:

**Layout change — from flat rows to left/right bubbles:**

AI messages (role === 'assistant'):
```
<motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2, ease: 'easeOut' }}>
  <div className="flex gap-3 px-4 py-3">
    <div className="h-8 w-8 rounded-full bg-zinc-700 flex items-center justify-center flex-shrink-0">
      <Bot className="h-4 w-4 text-[#bfdbfe]" />
    </div>
    <div className="rounded-xl rounded-tl-none bg-zinc-800 border border-zinc-700 px-4 py-3 max-w-[80%]">
      <p className="text-sm text-zinc-200 leading-relaxed whitespace-pre-wrap">{text}</p>
    </div>
  </div>
</motion.div>
```

User messages (role === 'user'):
```
<motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2, ease: 'easeOut' }}>
  <div className="flex gap-3 px-4 py-3 justify-end">
    <div className="rounded-xl rounded-tr-none bg-[#bfdbfe]/10 border border-[#bfdbfe]/20 px-4 py-3 max-w-[80%]">
      <p className="text-sm text-zinc-200 leading-relaxed whitespace-pre-wrap">{text}</p>
    </div>
  </div>
</motion.div>
```

**Imports:**
- Add `import { motion } from 'framer-motion'`
- Add `import { Bot } from 'lucide-react'`
- Remove any existing row separator styles (`border-b border-zinc-100 py-6`)
- Remove `bg-zinc-100` avatar circles, `text-zinc-700` body, `text-indigo-600` icon references

**Image attachments:** If the existing component handles image parts, keep that logic but wrap in the bubble styling.

**IMPORTANT:** Preserve the existing logic for extracting text from UIMessage.parts (AI SDK v6 uses parts array, not .content string). Only change the visual rendering, not the data extraction.
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Chat messages render as left/right bubbles. AI: dark bg-zinc-800 bubble with Bot avatar on left. User: pastel blue tint bubble on right. Each message animates in with fadeIn+slideUp via framer-motion. No flat row separators, no light surfaces, no indigo colors remain.</done>
</task>

<task type="auto">
  <name>Task 2: Chat wizard header + stepper + typing indicator + dark input</name>
  <files>
    ity/apps/web/app/(dashboard)/a/landing/chat/page.tsx
    ity/apps/web/components/chat/chat-wizard.tsx
    ity/apps/web/components/chat/chat-input.tsx
  </files>
  <action>
Read each file first, then apply:

**chat/page.tsx — Add page wrapper with header:**
- Wrap existing ChatWizard in a flex column container: `<div className="flex h-full flex-col">`
- Add header bar above ChatWizard:
  ```
  <div className="flex-shrink-0 border-b border-zinc-700 bg-zinc-900/50 backdrop-blur-sm px-4 py-3 flex items-center justify-between">
    <div>
      <p className="text-xs text-zinc-500">Creando landing para</p>
      <p className="text-sm font-semibold text-zinc-100">{schoolName || 'Tu escuela'}</p>
    </div>
    <div className="flex items-center gap-4">
      {/* Stepper rendered here or passed to ChatWizard */}
      <button onClick={handleClose} className="text-zinc-400 hover:text-zinc-200 transition-colors p-1 rounded-lg hover:bg-zinc-800" aria-label="Cerrar">
        <X className="h-5 w-5" />
      </button>
    </div>
  </div>
  ```
- Import `X` from lucide-react for the close button.
- `handleClose` should navigate back to the landing page (e.g., `router.push('/a/landing/templates')` or `router.back()`). Use Next.js `useRouter` if this is a client component, or wrap header in a client component if page is RSC.
- Get schoolName from the existing server data if available (this page is RSC — check what data it already fetches). If not available, use 'Tu escuela' as fallback.

**chat-wizard.tsx — Progress stepper + typing indicator + dark banners:**

Progress stepper component (inline, simple):
- Track step number: `const currentStep = Math.min(Math.ceil(userTurnCount / 3), 5)` where userTurnCount = messages.filter(m => m.role === 'user').length
- Render 5 step dots/circles: completed steps get `bg-[#bfdbfe]`, current step gets `bg-[#bfdbfe] ring-2 ring-[#bfdbfe]/30`, future steps get `bg-zinc-700`
- Small connecting lines between dots: `bg-zinc-700` (completed: `bg-[#bfdbfe]`)
- Place stepper in the header area or top of wizard

Typing indicator:
- Show when `isLoading` is true (useChat's loading state)
- Render as an AI-style bubble (left-aligned, bg-zinc-800) containing three dots:
  ```
  <div className="flex gap-1 items-center py-1 px-1">
    {[0, 1, 2].map((i) => (
      <span key={i} className="h-2 w-2 rounded-full bg-zinc-400 animate-pulse" style={{ animationDelay: `${i * 150}ms` }} />
    ))}
  </div>
  ```
- Place this after the last message in the messages list, only when isLoading

Dark banners:
- Rate limit banner: Replace `bg-amber-50 text-amber-700` → `bg-zinc-800 border border-zinc-700 text-[#fef3c7]`
- Chat finished banner: Replace `bg-zinc-50 border-zinc-200` → `bg-zinc-800 border border-zinc-700 text-zinc-200`
- Retry/error: Replace `bg-red-50 text-red-600` → `bg-zinc-800 border border-zinc-700 text-[#fecaca]`

**chat-input.tsx — Dark input styling:**
- Container: `bg-zinc-800/50 border border-zinc-700 rounded-xl`
- Input/textarea: `bg-transparent text-zinc-100 placeholder:text-zinc-500`
- Focus: `focus-within:border-[#bfdbfe] focus-within:ring-2 focus-within:ring-[#bfdbfe]/30`
- Send button: `bg-[#bfdbfe] text-zinc-900 hover:bg-[#93c5fd] rounded-lg p-2 transition-colors disabled:opacity-50`
- Replace any `bg-zinc-50`, `border-zinc-200`, `focus-within:border-indigo-300`, `focus-within:ring-indigo-200`, `bg-indigo-600`
  </action>
  <verify>
    <automated>cd ity && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Chat page has header with school name and close button (X icon navigating back). Progress stepper shows 5 steps with current highlighted in pastel blue. Typing indicator shows 3 pulsing dots during AI response. Chat input is dark with pastel blue send button. All banners (rate limit, finished, error) use dark zinc palette. No light surfaces (zinc-50, amber-50, red-50) or indigo colors remain.</done>
</task>

</tasks>

<verification>
- Grep for `indigo` in all 4 chat files — should return 0 matches
- Grep for `zinc-50` in chat-wizard.tsx and chat-input.tsx — should return 0 matches
- Grep for `amber-50` in chat-wizard.tsx — should return 0 matches
- Grep for `border-zinc-100` in chat-message.tsx — should return 0 matches
- Grep for `motion` in chat-message.tsx — should return matches (entry animation present)
- TypeScript compiles without errors
</verification>

<success_criteria>
Chat wizard feels premium: bubble messages with animations, visible progress, typing indicator, dark input. No flat rows, no light surfaces, no indigo accents. The chat experience matches the dark theme of the rest of the app.
</success_criteria>

<output>
After completion, create `.planning/phases/11.5-ui-refinement/11.5-P03-SUMMARY.md`
</output>
